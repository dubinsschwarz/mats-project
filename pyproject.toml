[project]
name = "mats-project"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "nnsight>=0.7.0",
    "torch>=2.13.0",
    "transformer-lens>=3.8.1",
]

[dependency-groups]
dev = [
    "jupyter-collaboration>=5.0.2",
    "jupyter-mcp-tools>=0.1.7",
]
